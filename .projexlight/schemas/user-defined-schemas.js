// MongoDB Schema Definitions
// Generated by ProjexLight CLI Export System
// Use with Mongoose ODM

const mongoose = require('mongoose');
const { Schema } = mongoose;

// ========================================
// Schema: Leadpulse MCP Schema
// Description: Auto-generated database schema for Leadpulse MCP. Generated from project description...
// ========================================

// Users Schema
const UsersSchema = new Schema({
  email: {
    type: String,
  },
  password_hash: {
    type: String,
  },
  created_at: {
    type: Date,
  },
}, {
  timestamps: { createdAt: 'created_at', updatedAt: 'updated_at' },
  collection: 'users'
});

const Users = mongoose.model('Users', UsersSchema);

// Campaigns Schema
const CampaignsSchema = new Schema({
  title: {
    type: String,
  },
  content: {
    type: String,
  },
  scheduled_time: {
    type: Date,
  },
  status: {
    type: String,
    default: "draft",
  },
}, {
  timestamps: { createdAt: 'created_at', updatedAt: 'updated_at' },
  collection: 'campaigns'
});

const Campaigns = mongoose.model('Campaigns', CampaignsSchema);

// Contacts Schema
const ContactsSchema = new Schema({
  email: {
    type: String,
  },
  name: {
    type: String,
  },
  status: {
    type: String,
    default: "active",
  },
}, {
  timestamps: { createdAt: 'created_at', updatedAt: 'updated_at' },
  collection: 'contacts'
});

const Contacts = mongoose.model('Contacts', ContactsSchema);

// Campaign_status Schema
const Campaign_statusSchema = new Schema({
  campaign_id: {
    type: String,
  },
  sent_at: {
    type: Date,
  },
  status: {
    type: String,
  },
  error_message: {
    type: String,
  },
}, {
  timestamps: { createdAt: 'created_at', updatedAt: 'updated_at' },
  collection: 'campaign_status'
});

const Campaign_status = mongoose.model('Campaign_status', Campaign_statusSchema);

module.exports = {
  Users,
  Campaigns,
  Contacts,
  Campaign_status,
};
